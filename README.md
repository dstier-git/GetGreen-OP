# GetGreen-OP

Repo for GetGreen.ai (OP Fall 25).

## Run the app

See **core/README.md** for backend, frontend, and data setup.

## Git

- **Pull:** `git pull origin main`
- **Push changes:** `git add <file>` → `git commit -m "message"` → `git push origin main`
